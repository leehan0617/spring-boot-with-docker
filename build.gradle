buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.springframework.boot:spring-boot-gradle-plugin:2.3.4.RELEASE"
    }
}

plugins {
    id 'java'
    id 'org.springframework.boot' version '2.3.4.RELEASE'
    // 이걸 안쓰면 jpa 설정할때 ClassNotFoundException javax.xml.bind.JAXBException 발생
    id 'io.spring.dependency-management' version '1.0.8.RELEASE'
}

group 'org.example'
version '1.0-SNAPSHOT'

sourceCompatibility = JavaVersion.VERSION_11

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.springframework.boot', name: 'spring-boot-starter', version: '2.3.4.RELEASE'
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-web', version: '2.3.4.RELEASE'
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-data-jpa', version: '2.3.4.RELEASE'
    runtimeOnly group: 'mysql', name: 'mysql-connector-java', version: '8.0.21'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

// 12라인의 플러그인때문에 bootJar이라는 task를 사용할 수 있으므로 아래 설정은 불필요.
//jar {
//    manifest {
//        attributes 'Title': 'SpringBootDockerExample', 'Version': '0.0.1', 'Main-Class': 'com.heron.lee.Application'
//    }
//    from configurations.runtime.collect { zipTree(it) }
//}
